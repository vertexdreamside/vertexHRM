export default function Page() {
  return (
    <div>
      <h1 className="font-display text-2xl font-medium text-ink">Documents</h1>
      <p className="mt-1 text-sm text-ink-muted">
        Not built yet — see Admin Ops spec §2 for the full spec this screen
        implements.
      </p>
    </div>
  );
}
