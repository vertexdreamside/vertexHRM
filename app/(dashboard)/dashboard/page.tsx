export default function DashboardPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-medium text-ink">
        Dashboard
      </h1>
      <p className="mt-1 text-sm text-ink-muted">
        This is a placeholder — the real dashboard tiles (pending approvals,
        leave balances, open IT tickets) come from the Reports module spec
        once the underlying modules are built.
      </p>
    </div>
  );
}
